[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "box2d-py>=2.3.8",
    "gymnasium==0.29.1",
    "jupyter>=1.1.1",
    "matplotlib>=3.10.7",
    "numpy>=1.23",
    "optuna>=3.6",
    "pygame>=2.6.1",
    "stable-baselines3==2.3.0",
    "tensorboard>=2.14",
    "torch==2.4.0 ; platform_machine == 'arm64' and sys_platform == 'darwin'",
    "torch>=2.0",
    "torch>=2.0 ; platform_machine != 'arm64' or sys_platform != 'darwin'",
]
